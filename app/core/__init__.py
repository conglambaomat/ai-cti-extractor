"""Core layer: config, logging, security, exceptions, redaction.

This package is dependency-free of other ``app.*`` modules so it can be
imported from anywhere without circular-import risk.
"""

from __future__ import annotations
